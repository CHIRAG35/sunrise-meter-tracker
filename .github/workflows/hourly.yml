name: Hourly Meter Logger

on:
schedule:
- cron: '0 * * * *'
workflow_dispatch:

jobs:
log-meter:
runs-on: ubuntu-latest

```
steps:
  - name: Test Run
    run: |
      echo "Hourly Meter Logger Running"
      date
```
